<?xml version="1.0" encoding="utf-8"?>
<manifest xmlns:android="http://schemas.android.com/apk/res/android"
    xmlns:tools="http://schemas.android.com/tools">

    <!-- Required Permissions -->
    <uses-permission android:name="android.permission.INTERNET"/>
    <uses-permission android:name="android.permission.CAMERA"/>
    <uses-permission android:name="android.permission.WRITE_EXTERNAL_STORAGE"/>
    <uses-permission android:name="android.permission.ACCESS_NETWORK_STATE"/>
    <uses-permission android:name="android.permission.READ_EXTERNAL_STORAGE"/>

    <!-- OpenCV and Camera Features -->
    <uses-feature android:name="android.hardware.camera" android:required="true"/>
    <uses-feature android:name="android.hardware.camera.autofocus" android:required="false"/>
    <uses-feature android:name="android.hardware.camera.front" android:required="false"/>
    <uses-feature android:name="android.hardware.camera.front.autofocus" android:required="false"/>

    <application
        android:name=".MyApp"
        android:usesCleartextTraffic="true"
        android:allowBackup="true"
        android:icon="@mipmap/ic_launcher"
        android:label="@string/app_name"
        android:roundIcon="@mipmap/ic_launcher_round"
        android:supportsRtl="true"
        android:theme="@style/Theme.Nutrifill"
        android:enableOnBackInvokedCallback="true"
        android:hardwareAccelerated="true"
        tools:targetApi="31">

        <!-- Backup & Data Extraction -->
        <meta-data
            android:name="com.android.backup.autoRestore"
            android:value="true"/>

        <!-- Main Activity (Launcher) -->
        <activity
            android:name=".WelcomeActivity"
            android:exported="true">
            <intent-filter>
                <action android:name="android.intent.action.MAIN"/>
                <category android:name="android.intent.category.LAUNCHER"/>
            </intent-filter>
        </activity>

        <!-- Signup Activity -->
        <activity android:name=".SignupActivity" android:exported="false"/>

        <!-- Sign In Activity -->
        <activity android:name=".FstActivity" android:exported="false"/>

        <!-- User Details Activity -->
        <activity android:name=".UserDetailsActivity" android:exported="false"/>

        <!-- Scan Activity -->
        <activity 
            android:name=".ScanActivity" 
            android:exported="false"
            android:hardwareAccelerated="true"/>

        <!-- Nutrition History Activity -->
        <activity android:name=".NutritionHistoryActivity" android:exported="false"/>

        <!-- Home Activity -->
        <activity
            android:name=".HomeActivity"
            android:exported="false"
            android:label="@string/app_name"/>

        <!-- Nutrition Summary Activity -->
        <activity android:name=".NutritionSummaryActivity" android:exported="false"/>

        <!-- Recommendations Activity -->
        <activity android:name=".RecommendationsActivity" android:exported="false"/>

        <!-- Scanner Activity -->
        <activity 
            android:name=".ScannerActivity" 
            android:exported="false"
            android:hardwareAccelerated="true"/>

        <!-- Food Details Activity -->
        <activity 
            android:name=".FoodDetailsActivity" 
            android:exported="false"/>

        <!-- Nutrition Tracking Activity -->
        <activity
            android:name=".NutritionTrackingActivity"
            android:exported="false"/>

        <!-- Food Scanner Activity -->
        <activity
            android:name=".FoodScannerActivity"
            android:exported="false"
            android:hardwareAccelerated="true"/>

        <!-- Manual Food Entry Activity -->
        <activity
            android:name=".ManualEntryActivity"
            android:exported="false"/>

        <!-- Food Entry Activity -->
        <activity
            android:name=".activities.FoodEntryActivity"
            android:exported="true"
            android:label="Manual Food Entry" />

        <activity
            android:name=".AmmaUnavagamActivity"
            android:exported="false" />



        <!-- BMI Result Activity -->
        <activity
            android:name=".BMIResultActivity"
            android:exported="false" />


    </application>
</manifest>
